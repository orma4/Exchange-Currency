import React from "react";
import GenericPage from './GenericPage';

const EuroToUsd = () => {
  return <GenericPage storeKey = 'eurToUsd' base = 'EUR' dest = 'USD' />;
};

export default EuroToUsd;
